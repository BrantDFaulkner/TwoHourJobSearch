<%= form_for(@company) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :motivation %>
  <%= f.select :motivation, options_for_select(1..5) %>
  <%= f.submit "Create" %>
<% end %>